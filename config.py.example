# --- Centralized Configuration File (Example) ---

# This is an example configuration file.
# 1. Rename this file to `config.py`.
# 2. Fill in your actual credentials.
# 3. Make sure `config.py` is listed in your `.gitignore` file to keep your secrets safe.

import os

# Finlight API Key
# You can get a free API key from https://finlight.me/
# It is recommended to set this as an environment variable for security.
FINLIGHT_API_KEY = os.getenv("FINLIGHT_API_KEY", "YOUR_API_KEY_HERE")

# Reddit API Credentials
# It is recommended to set these as environment variables for security.
REDDIT_CLIENT_ID = os.getenv("REDDIT_CLIENT_ID", "YOUR_REDDIT_CLIENT_ID")
REDDIT_CLIENT_SECRET = os.getenv("REDDIT_CLIENT_SECRET", "YOUR_REDDIT_CLIENT_SECRET")
REDDIT_USER_AGENT = os.getenv("REDDIT_USER_AGENT", "YOUR_REDDIT_USER_AGENT")

# Twitter API Credentials
# It is recommended to set this as an environment variable for security.
# For this project, only the App-only Bearer Token is required.
TWITTER_BEARER_TOKEN = os.getenv("TWITTER_BEARER_TOKEN", "YOUR_TWITTER_BEARER_TOKEN")

# Stock Tickers to Track
# A list of stock tickers that the pipeline will process.
TICKERS = ["AAPL", "TSLA", "NVDA", "JPM", "AMZN"]

# Subreddits to search for ticker mentions
# A list of subreddits to search for ticker mentions.
SUBREDDITS = ["wallstreetbets", "stocks", "investing"]

# Database Connection Details
# It is recommended to use environment variables for these settings.
DB_NAME = os.getenv("DB_NAME", "stock_sentiment")
DB_USER = os.getenv("DB_USER", "postgres")
DB_PASSWORD = os.getenv("DB_PASSWORD", "password")
DB_HOST = os.getenv("DB_HOST", "localhost")
DB_PORT = os.getenv("DB_PORT", "5432")

# Database Table Name
DB_TABLE_NAME = "stock_data"

# Data directory
PROCESSED_CSV_PATH = "data/processed_data.csv"
